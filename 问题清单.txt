怎么保证非关键流量不妨碍关键流量的传输，并且问题与时间感知调度中的两个队列和门有什么关系。
树列表:
time
    --ptp(时间同步)
    --global(ptp模式，同步上下界(ns),状态(是否同步，最大偏移绝对值，ptp  time))
    --边界同步
        --global    
        --port(port列表，ptp开启/关闭,          ptp状态，同步。。，延时机制，vlan，VLAN优先级)    
        变化：上上个global是否同步打勾，非0偏移量

TSN
    --门控列表(tsn端口显示，分别显示门状态，间隔)        

